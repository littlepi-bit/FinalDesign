syntax = "proto3";

message HanlpRequest {
    string str1 = 1;
    string str2 = 2;
}

message HanlpReply {
    string Message = 1;
}

service HanlpServer {
    rpc SayHello(HanlpRequest) returns (HanlpReply) {}
    //获取两个文本的语义相似度
    rpc Similarity(HanlpRequest) returns (HanlpReply) {}
    //
}
